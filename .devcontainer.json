{
  "name": "Laravel Codespace",
  "image": "mcr.microsoft.com/devcontainers/php:8.2",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    },
    "ghcr.io/devcontainers/features/composer:1": {}
  },
  "postCreateCommand": "apt-get update && apt-get install -y default-mysql-server && service mysql start && mysql -e \"ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root'; FLUSH PRIVILEGES;\" && composer install && npm install && cp .env.example .env && php artisan key:generate",
  "forwardPorts": [8000],
  "remoteUser": "vscode"
}
